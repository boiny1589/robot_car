<launch>
    <!-- 设置地图的配置文件 -->
    <arg name="map" default="map.yaml" />  <!--map_server.launch生成的是 nav.yaml-->
    <!-- 运行地图服务器，并且加载设置的地图-->
    <node name="map_server" pkg="map_server" type="map_server" args="$(find nav_ma)/map/$(arg map)"/>
</launch>
